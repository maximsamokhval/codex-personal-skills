interface:
  display_name: "Requirements Critic"
  short_description: "Review requirements JSON before approval"
  default_prompt: "Use $requirements-critic to review requirements.json, preserve it unchanged, and create review.json."
